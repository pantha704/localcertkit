{
  "name": "certkit-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "CertKit static site — browser-local certificate and key tools.",
  "scripts": {
    "sync": "node ../scripts/sync-site-lib.mjs",
    "dev": "node ../scripts/sync-site-lib.mjs && astro dev",
    "build": "node ../scripts/sync-site-lib.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "6"
  }
}
